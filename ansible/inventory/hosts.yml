---
# Ansible Inventory for sec-levels
# Purpose: Define target hosts for CIS hardening

all:
  children:
    test_environments:
      hosts:
        docker-kernel68:
          ansible_host: sec-levels-test-68
          ansible_connection: docker
          ansible_user: secuser
        docker-latest:
          ansible_host: sec-levels-test-latest
          ansible_connection: docker
          ansible_user: secuser

    production:
      hosts:
        # Add production hosts here
        # prod-server1:
        #   ansible_host: 192.168.1.10
        #   ansible_user: admin

  vars:
    ansible_python_interpreter: /usr/bin/python3
